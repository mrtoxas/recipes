{
  "name": "astro-cloudflare-hono-openapi",
  "description": "Example of an Astro SSR project on Cloudflare with an API endpoint extended using Hono and OpenAPI",
  "author": "Anton Bojko (https://github.com/mrtoxas)",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler pages dev",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.6",
    "@astrojs/cloudflare": "^12.6.12",
    "astro": "^5.16.7",
    "chanfana": "^3.0.0",
    "hono": "^4.11.3",
    "typescript": "^5.9.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "wrangler": "^4.58.0"
  }
}
