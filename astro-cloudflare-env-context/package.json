{
	"name": "astro-cloudflare-env-context",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"build": "wrangler types && astro check && astro build",
		"preview": "astro build && wrangler pages dev",
		"deploy": "wrangler types && astro check && astro build && wrangler pages deploy ./dist",
		"astro": "astro",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@astrojs/check": "^0.9.6",
		"@astrojs/cloudflare": "^12.6.12",
		"astro": "^5.16.5",
		"typescript": "^5.9.3"
	},
	"devDependencies": {
		"@types/node": "^24.10.2",
		"wrangler": "^4.53.0"
	}
}